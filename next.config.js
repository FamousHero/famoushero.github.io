/** @type {import('next').NextConfig} */

const nextConfig = {
    output: "export",
    reactStrictMode: true,
}

module.exports = nextConfig
// */
// const nextConfig = {}
// module.exports = nextConfig
